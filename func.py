def multi(*args):
    return ([i/2 for i in args])
  
 def decorador(fn)
     def nuevo(*args):
         for i, arg in enumee(*args):
            print "arg: %d%d" % (i, args)
          return fn(*args)
     return nuevo

